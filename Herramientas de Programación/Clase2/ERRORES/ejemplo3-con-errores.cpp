include <conio.h>

#include <stdio.h>

main()

{ int a, b, c; a = 5; b = ++a; */++a significa que le suma uno al valor contenido en la variable a/* c = ( a + 5 * 2 ) * ( b + 6 / 2 ) + ( a * 2 ); printf( "%i; %i, %i", a, b, &c ); }
